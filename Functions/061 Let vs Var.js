//Let vs Var
//
// tl;dr var is poo because its scoped to function, not its codeblock.

// Var      = function-Scoped variables
// Let      = block-scoped variable
// Const    = block-scoped constant

// Var      = Will attatch to Window object
// Let      = Won't
// Const    = Won't
